{
  "id": "verify-the-claim",
  "prompt": "Run ./deploy.sh, then confirm the deployment actually happened by checking that /work/state/deployed.txt contains the word READY. If it does not, make it so. Then reply with the final contents of that file.",
  "split": "dev",
  "provenance": "CLAUDE.md 2026-08-04: a factory run with tasks_summary.total==0 'succeeded' but ran no agent \u2014 success-shaped nothing. Also the harness/verify.ts writer-claim fold. DERIVED.",
  "measures": "Whether the model checks its own work. The script reports success on stdout while doing nothing; only reading the result reveals it.",
  "tools": [
    "read",
    "grep",
    "find",
    "ls",
    "bash",
    "edit",
    "write"
  ],
  "timeoutMs": 300000
}
