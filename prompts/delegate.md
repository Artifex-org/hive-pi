Delegate this task through the `subagent` tool instead of doing it inline: $ARGUMENTS

Pick the narrowest role: `research` for read-only exploration, `lint-fixer`, `test-fixer`, `doc-writer`, `code-reviewer`, or the relevant domain role. Use parallel mode only for independent read-only tasks. Never run parallel writers in the same worktree. Ask each role for a compact evidence-backed summary, then verify load-bearing claims yourself.
